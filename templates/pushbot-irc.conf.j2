respawn

exec su - {{ pushbot_username }} -c 'cd {{ pushbot_root }} ; bin/pushbot -a irc -n {{ pushbot_irc_nick }} >> {{ pushbot_log }}/pushbot.irc.log 2>&1'

start on runlevel [2345]

stop on runlevel [016]
